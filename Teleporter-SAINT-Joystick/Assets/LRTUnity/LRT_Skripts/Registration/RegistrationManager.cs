using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This class handles the registration of all objects, which contribute to the 3D-Reconstruction or to help predicting dynamic objects
/// At the moment  the registration is static and no dynamic object recongnition is implemented
/// </summary>
public class RegistrationManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
